fn main() {
    //array of numbers
    let numbers = [1,2,3,4,];
    println!("Original array={:?}", numbers);

    //creta a slice of 2nd and 3rd element
    let slice1 = &numbers[1..3];
    println!("2nd and 3rd elements sliced ={:?}", slice1);

    //omit the start index
    let slice2 
}
